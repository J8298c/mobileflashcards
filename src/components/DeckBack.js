import React from 'react';
import { Text, View } from 'react-native';

const DeckBack = (props) => (
    <View>
        <Text>
            {this.props.answer}
        </Text>
    </View>
)

export default DeckBack;